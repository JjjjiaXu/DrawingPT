# DrawingPT：工程图纸的自监督预训练
## 项目启动文档（学生版 · 可分享）

> 面向：合作同学（技术指导：Tao）
> 版本：v1 · 2026-08-12 · 有问题随时在群里 @ Tao

---

## 0. 一分钟看懂

- **我们做什么**：给**2D 工程图纸（DWG/矢量 CAD：建筑施工图、电气图）**做一个**自监督预训练模型**——把图纸从"一堆线段和文字"变成"可计算、可检索、可算量的结构化工程对象"。下游任务：图元/构件识别（panoptic symbol spotting）、语义分割、构件计数与算量。
- **为什么现在做**（2026-08 两轮系统检索的结论）：
  1. **"工程图纸 foundation model" 目前不存在**——3D 机械 CAD 已有人做（Brep2Shape、UV-Net），2D 工程图只有任务专用小模型；
  2. **通用 VLM（GPT-4V/Gemini）直读工程图做精确计数/测量仍不可靠**（ArchPlanVQA 等 2026 基准的普遍结论）——"矢量结构化 + 领域模型"路线的窗口还在；
  3. **配方是现成的**：THUML 的 Brep2Shape（ICML 2026）证明了"参数化基元 token 化 + 双流 Transformer + 拓扑注意力 + 自监督对齐"这套配方，用 **1.3–3.3M 参数的小模型**就能在 CAD 理解上全面 SOTA——我们把这套配方从 3D B-rep 移植到 2D 矢量图元。
- **目标产出**：一篇可投 **CVPR / ICCV / NeurIPS**（或 D&B track）的论文；同时为造价算量应用打技术底座（应用侧数据对接由老师协调，见 §1 纪律）。
- **算力**：**单卡即可起步**（对标模型只有百万级参数；FloorPlanCAD 全量 1.5 万张图）。不需要等大算力。

---

## 1. 红线与纪律 ⚠️（先读这段）

> [!IMPORTANT]
> **✅ 我们要证明的（站得住）**：
> 1. **自监督预训练在工程图上有效**——在等量标注下，预训练模型在 symbol spotting / 分割上打过从零训练的基线（对标 GeoPT 的"标注需求降 20–60%"叙事）；
> 2. **矢量原生路线 > 栅格/VLM 路线**——用同一评测对比 VLM 直读（要实测，不能只引用别人的负面结论）；
> 3. **双流设计有增益**——工程图的语义一半在标注文字里，图元流+文字流的消融要做干净。
>
> **⛔ 我们绝不主张的**：
> 1. "**首个 CAD 理解模型**"——3D 侧 Brep2Shape/UV-Net/BRT 已占位，2D 侧 FloorPlanCAD 一系（ICCV 2021 起）已有五年积累，我们是"首个**2D 工程图**的**通用自监督预训练**"这个更窄的主张，且投稿前要再做一轮撞车检索；
> 2. "VLM 做不了所以我们赢"——VLM 在进步，论文的价值必须落在"矢量结构先验 + 数据效率"上，不是落在踩 VLM；
> 3. 任何涉及具体商业客户、合同、收入的表述——**论文与对外材料里一律不出现**。

> [!WARNING] 数据与代码纪律（不可协商，保护你自己）
> 1. **现阶段只用公开数据集**（FloorPlanCAD 等）。业务方的真实图纸数据**在数据协议签署落地之前一律不碰**——什么时候能用、怎么用，由老师通知；
> 2. **代码从第一天起就开独立学术仓库**，不与任何公司代码混放——学术成果（你的论文、你的毕业材料）与产品代码分库、分许可，混了以后拆不开；
> 3. 你的**论文署名与毕业成果优先**——这个项目首先是你的学术产出，其次才是应用。

---

## 2. 技术方案骨架：Brep2Shape 配方的 2D 移植

Brep2Shape（arXiv 2602.07429）在 3D B-rep 上的四件套，逐项映射到 2D 矢量图纸：

| Brep2Shape（3D B-rep） | DrawingPT（2D DWG，我们做的） |
|---|---|
| 曲面/曲线 → 标准化 Bézier 基元 token | 线/弧/多段线/圆/块引用 → 标准化图元 token（DWG 本身就是参数化矢量，天然可 token 化） |
| 面流 + 边流双 Transformer | **图元流 + 标注/文字流**双 Transformer（工程图语义一半在标注里——这是 2D 侧的新问题，也是我们的贡献点） |
| 拓扑注意力（B-rep 邻接） | 图层 / 块引用 / 空间邻接注意力 |
| 边界→形状自监督对齐（控制点 → 稠密采样点） | **矢量→栅格渲染对齐**（矢量图元预测其栅格渲染，天然免标注）；后续可加图元→构件语义的弱监督 |
| 预训练语料：25 万 B-rep（8 源聚合） | 第一阶段：FloorPlanCAD + 可收集的公开 DWG；第二阶段（协议落定后）：领域私有语料 |

**模型量级**：对标 Brep2Shape 的 6 层 / 4 头 / 128 维、总参数 1.3–3.3M——**先做小**，把配方验证清楚比堆参数重要。

---

## 3. 必读清单（第一周读完，各写 10 行笔记）

| 论文 | 为什么读 |
|---|---|
| **FloorPlanCAD**（ICCV 2021，arXiv 2105.07147） | 本领域标准 benchmark：1.5 万张真实矢量图、35 类符号、panoptic symbol spotting 任务定义——**我们的主评测就是它** |
| **CADSpotting**（arXiv 2412.07377，CVPR 2025 线） | 当前最强基线之一（密集采样点表示图元），我们要复现的靶子 |
| **Brep2Shape**（arXiv 2602.07429，ICML 2026） | 配方来源：token 化、双流、拓扑注意力、自监督对齐怎么设计 |
| **GeoPT**（arXiv 2602.20399，ICML 2026） | "预训练降低下游标注需求"的实验设计范本（怎么画标注效率曲线、怎么做 scaling 消融） |
| **HouseMind**（arXiv 2603.11640） | MLLM 读平面图的对手路线——知道 VLM 派怎么做，才能设计公平对比 |
| ArchPlanVQA（ASCE JCCE 2026）| VLM 在工程图上的失败模式——我们的对比实验要覆盖它指出的计数/测量弱点 |

---

## 4. 里程碑（今日 2026-08-12 起）

| 时间 | 里程碑 | 检查点 |
|---|---|---|
| 第 1 周（~08-19） | 环境就绪；FloorPlanCAD 下载 + 数据格式吃透；必读清单读完 | 能画出一张图的图元分布统计 |
| 第 2–3 周 | **复现基线**：FloorPlanCAD 上跑通 1–2 个公开基线（CADSpotting 或其开源前作），拿到与论文数字对得上的结果 | 基线对照表（指标 + runtime + 硬件）|
| 第 4–6 周 | **DrawingPT v0**：图元 token 化 + 矢量→栅格自监督预训练小规模跑通；微调后与"从零训练"对比 | 一张「预训练 vs 从零」的标注效率曲线——**这张图决定项目生死** |
| 第 7–8 周 | 双流（+文字）消融 + VLM 对比实验 | 若预训练无增益 → 与老师讨论转向或止损 |
| ~10 月 | 论文骨架 + 是否上私有语料的决策点 | — |

> **止损规则**：第 6 周的标注效率曲线若显示预训练增益 < 5%（相对从零训练），先停下来分析原因再扩规模——不盲目堆数据和参数。

---

## 5. 第一周动手清单

**第 1 步 · 环境 + 数据（1–2 天）**
- [ ] 建独立 GitHub 仓库（学术仓库，见 §1 纪律）；
- [ ] 下载 **FloorPlanCAD**（floorplancad.github.io），跑通其数据加载；统计图元类型/数量分布，确认 35 类符号标注完整；
- [ ] 调研 DWG→矢量解析工具链（`ezdxf` 等），确认能从 DXF 里拿到图元参数与文字标注。

**第 2 步 · 基线选型（2–3 天）**
- [ ] 调研 FloorPlanCAD 榜上开源实现（PanCADNet / GAT-CADNet / SymPoint / CADSpotting 等），选 1–2 个**有代码、单卡可跑**的复现；
- [ ] 记录：论文指标 vs 复现指标 vs runtime——差距超过 2 个点要找原因。

**第 3 步 · 读文献 + 写第一份 note（并行）**
- [ ] §3 清单读完，每篇 10 行笔记：它做了什么 / 我们借什么 / 我们不同在哪；
- [ ] 写一页纸《DrawingPT v0 设计草案》：token 化方案、自监督目标、预训练语料量估计——下次组会讨论。

---

## 6. 一句话记住

> **我们赢在"矢量原生的结构先验 + 自监督省标注 + 干净的消融"，不赢在"更大更全首个"。先复现，再预训练，第 6 周的那张曲线说了算。**
